import React from "react";
import "./Header.css";

const Header = props => (
  <header className="header">
    <h1>ShuffleBoston!</h1>
    <h2>
      Click on a Boston Terrier image to earn points.  Don't click the Boston twice or you lose the game!
    </h2>
  </header>
);

export default Header;
